import styled from "styled-components"

export const Title = styled.h2 `
    font-family: 'Oswald', sans-serif;
    font-weight: 200;
    font-size: 2em;
    color: #f6921d;

    &:hover {
        cursor: pointer;
        color: #f8a13d;
    }
`